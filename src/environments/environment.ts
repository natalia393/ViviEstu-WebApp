export const environment = {
    //production: false,
    //base: 'http://localhost:8080'

    production: true, // Cambiamos a true porque ya está en la nube
    base: 'https://viviestuv2.onrender.com' // Solo el dominio, sin la parte de swagger
}